name: Deploy to Railway
on:
  push:
    branches: [main]
# Désactivé — Railway se déploie automatiquement via webhook GitHub
# Ce workflow est redondant
jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Railway auto-deploy via webhook — no action needed"
